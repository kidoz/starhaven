[project]
name = "starhaven-tooling"
version = "0.1.0"
description = "Python tooling for the StarHaven project"
requires-python = ">=3.14,<3.15"
dependencies = [
    "mkdocs>=1.6.1,<2",
]

[tool.uv]
package = false
